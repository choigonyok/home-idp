apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "home-idp.fullname" . }}-test-connection"
  labels:
    {{- include "home-idp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "home-idp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
